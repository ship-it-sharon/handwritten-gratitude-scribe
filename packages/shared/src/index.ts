// @posy/shared — domain types and logic shared between the web app (V1)
// and the Expo mobile app (V2). Populated beginning in M1; kept separate
// from day one so mobile reuse is a package import, not a refactor.

export {};
